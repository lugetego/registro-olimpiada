{% extends 'base.html.twig' %}

{% block title %}Estudiante{% endblock %}

{% block body %}
    <h1>Estudiante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estudiante.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estudiante.nombre }}</td>
            </tr>
            <tr>
                <th>Paterno</th>
                <td>{{ estudiante.paterno }}</td>
            </tr>
            <tr>
                <th>Materno</th>
                <td>{{ estudiante.materno }}</td>
            </tr>
            <tr>
                <th>Nivel</th>
                <td>{{ estudiante.nivel }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ estudiante.mail }}</td>
            </tr>
            <tr>
                <th>Nacimiento</th>
                <td>{{ estudiante.nacimiento ? estudiante.nacimiento|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Puntuacion</th>
                <td>{{ estudiante.puntuacion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estudiante_index') }}">back to list</a>

    <a href="{{ path('estudiante_edit', {'id': estudiante.id}) }}">edit</a>

    {{ include('estudiante/_delete_form.html.twig') }}
{% endblock %}
